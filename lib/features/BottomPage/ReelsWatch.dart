import 'package:flutter/material.dart';

class ReelsWatch extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.red,
      body: Column(
        children: [
          Center(
            child: Text('Reels Watch'),
          ),
        ],
      ),
    );
  }
}
